# MCTS tree search for complex reasoning
# Uses Monte Carlo Tree Search to explore solution space

model:
  type: vllm
  name: deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B
  gpu_memory_utilization: 0.9

sampler:
  type: mcts
  max_iterations: 50  # Number of MCTS iterations
  branch_factor: 3    # Children per expansion
  max_tokens: 512     # Max total generation length
  tokens_per_step: 32 # Tokens per expansion
  rollout_tokens: 64  # Tokens for rollout simulation
  temperature: 0.7
  exploration_constant: 1.414  # UCB exploration param

dataset:
  name: aime
  source: hf

evaluator:
  type: accuracy

run:
  batch_size: 1  # MCTS is sequential per problem
  n_samples: 1
  max_problems: 30  # AIME is smaller
  verbose: true

output:
  dir: results
  run_name: aime_mcts
